# DISABLED - Use manual deploy with: npm run deploy
# GitHub Actions workflow is disabled due to permission issues
# Use the following command for deployment:
# npm run deploy

# name: Deploy to GitHub Pages
# on:
#   push:
#     branches: [ main ]
# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#     - name: Checkout
#       uses: actions/checkout@v4
#       with:
#         fetch-depth: 0
#     - name: Setup Node.js
#       uses: actions/setup-node@v4
#       with:
#         node-version: '18'
#         cache: 'npm'
#     - name: Install dependencies
#       run: npm ci
#     - name: Build
#       run: npm run build
#     - name: Deploy to GitHub Pages
#       uses: peaceiris/actions-gh-pages@v3
#       with:
#         github_token: ${{ secrets.GITHUB_TOKEN }}
#         publish_dir: ./dist
#         force_orphan: true
